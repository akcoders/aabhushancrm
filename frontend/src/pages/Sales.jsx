import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {Plus,Search} from 'lucide-react';import api from '../api';import {Badge,Loading,Empty} from '../components/UI';import {money} from '../config'
export default function Sales(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[search,setSearch]=useState('');useEffect(()=>{const t=setTimeout(()=>api.get('/sales',{params:{search}}).then(r=>setRows(r.data.data)).finally(()=>setLoading(false)),250);return()=>clearTimeout(t)},[search]);return <div><div className="mb-6 flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-[#aa8131]">Commerce</p><h2 className="page-title mt-1">Sales & Invoices</h2><p className="mt-1 text-sm text-[#857d70]">Track showroom purchases and payments</p></div><Link to="/sales/create" className="btn-primary"><Plus size={17}/>Create sale</Link></div><div className="card overflow-hidden"><div className="border-b border-[#ece6da] p-4"><label className="relative block max-w-lg"><Search className="absolute left-3 top-2.5 text-[#9c9589]" size={18}/><input className="field pl-10" placeholder="Search invoice or customer…" value={search} onChange={e=>setSearch(e.target.value)}/></label></div>{loading?<Loading/>:!rows.length?<Empty/>:<div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead><tr className="bg-[#fcfbf8] text-[10px] uppercase tracking-wider text-[#888073]"><th className="px-5 py-3">Invoice</th><th>Customer</th><th>Jewellery</th><th>Sale date</th><th>Amount</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id} className="border-t border-[#eee9df]"><td className="px-5 py-4 text-sm font-semibold">{x.invoice_number}</td><td className="text-sm">{x.customer?.name}</td><td className="text-sm text-[#746d62]">{x.items?.[0]?.jewellery_type||'—'}</td><td className="text-sm text-[#746d62]">{new Date(x.sale_date).toLocaleDateString('en-IN')}</td><td className="font-semibold">{money(x.final_amount)}</td><td><Badge>{x.payment_status}</Badge></td></tr>)}</tbody></table></div>}</div></div>}
