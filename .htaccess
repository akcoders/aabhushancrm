Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger fallback when the domain document root points to the
    # Laravel project directory instead of the public directory.
    RewriteCond %{REQUEST_URI} !^/public(?:/|$)
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
